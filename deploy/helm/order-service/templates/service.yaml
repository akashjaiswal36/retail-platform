apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}

  selector:
    app: order-service

  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8082