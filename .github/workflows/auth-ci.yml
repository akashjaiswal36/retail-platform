name: Auth Service CI

on:
  push:
    branches:
      - main
      - feature/**
    paths:
      - 'services/auth-service/**'
      - '.github/workflows/**'

  pull_request:
    branches:
      - main
  
permissions:
  id-token: write
  contents: read

jobs:
  auth-pipeline:
    uses: ./.github/workflows/reusable-java-build.yml
    secrets: inherit