name: Frontend Service CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'services/frontend/**'
      - 'deploy/helm/frontend/**'
      - '.github/workflows/frontend-ci.yml'
      - '.github/workflows/reusable-java-build.yml'

permissions:
  id-token: write
  contents: read

jobs:
  build-test-scan:
    uses: ./.github/workflows/reusable-java-build.yml
    with:
      service-path: services/frontend-service
      ecr-repository: frontend
    secrets: inherit

  deploy:
    needs: build-test-scan
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ap-south-1
      EKS_CLUSTER_NAME: eks-demo-cluster
      RELEASE_NAME: auth-service
      NAMESPACE: retail-app

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::562887205095:role/GitHubActionsECRPushRole
          aws-region: ap-south-1

      - name: Install Helm
        uses: azure/setup-helm@v4


      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region ap-south-1 \
            --name eks-demo-cluster

      - name: Deploy to EKS
        run: |
          helm upgrade --install frontend ./deploy/helm/frontend \
            --namespace retail-app \
            --set image.tag=${{ github.sha }}