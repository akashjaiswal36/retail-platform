apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: retail-db-secret
  namespace: retail-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: ClusterSecretStore

  target:
    name: retail-db-secret
    creationPolicy: Owner

  data:
    - secretKey: DB_USERNAME
      remoteRef:
        key: retail/auth/dev/dbb
        property: username

    - secretKey: DB_PASSWORD
      remoteRef:
        key: retail/auth/dev/dbb
        property: password

    - secretKey: DB_HOST
      remoteRef:
        key: retail/auth/dev/dbb
        property: host

    - secretKey: DB_PORT
      remoteRef:
        key: retail/auth/dev/dbb
        property: port

    - secretKey: DB_NAME
      remoteRef:
        key: retail/auth/dev/dbb
        property: dbname